{
  "name": "@alter-eco/geo",
  "version": "0.0.4",
  "description": "",
  "scripts": {
    "mapshaper": "mapshaper src/*.geojson -simplify 10% keep-shapes -o dist/ format=topojson",
    "build": "rm -rf dist/* && npm run mapshaper",
    "prepublishOnly": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com:alter-eco/geo.git"
  },
  "author": "laurencedorman",
  "license": "ISC",
  "devDependencies": {
    "mapshaper": "^0.4.62"
  }
}
